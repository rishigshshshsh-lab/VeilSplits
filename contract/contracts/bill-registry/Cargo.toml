[package]
name = "bill-registry"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = { workspace = true }
stealth-pay = { path = "../stealth-pay" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }

[lib]
crate-type = ["cdylib", "rlib"]
